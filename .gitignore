data/
results/*.pkl
__pycache__/
*.pyc
.ipynb_checkpoints/
results/*.log
tests/fixtures/*/recording.csv
.DS_Store

# Per-fold evaluation models (~180 MB each). These exist only to score the held-out benchmark and
# are regenerated by:  python scripts/build_eval_benchmark.py --fold N --train-eval-model
# Committing all five would exceed the Git LFS free quota. (fold0 was committed before this rule
# and is left in place rather than rewriting history.)
results/model_rf_fold1.joblib
results/model_rf_fold2.joblib
results/model_rf_fold3.joblib
results/model_rf_fold4.joblib

# Core numerics / data
numpy>=1.26
pandas>=2.0
scipy>=1.11
joblib>=1.3

# Recognition backbone
scikit-learn>=1.6
# Model files are pickled by whichever scikit-learn fitted them, and loading them under a
# different version raises InconsistentVersionWarning. Verified with
# scripts/verify_model_portability.py that predictions are bit-identical across 1.6.0 and 1.9.0
# (same fingerprint, same probabilities to 6 dp), so the warning is safe to ignore in that range.

# Neural-net recognizers (TinyCNN / CNN+GRU / FeatureMLP); not needed to run the RF pipeline
torch>=2.2

# Query layer: intent parsing + grounded explanation via a local SLM
langchain-ollama>=1.0
langchain-core>=1.0
pydantic>=2.0
# Requires an Ollama server with qwen2.5:1.5b pulled. The pipeline falls back to deterministic
# templates automatically when Ollama is unreachable, so this is optional at run time.

# Figures, evaluation and cost measurement
matplotlib>=3.8
psutil>=5.9

# Data fetching and tests
requests>=2.31
tqdm>=4.66
pytest>=8.0
